{{- $frontendService := "" }}
{{- $gatewayService := "" }}
{{- $apiServices := list }}
{{- range $key, $val := .Values.services }}
{{- if and (hasKey $val "publishUrl") (ne $val.publishUrl "") }}
{{- if eq $val.publishUrl "/" }}
{{- $frontendService = $val }}
{{- else if eq $val.publishUrl "/api" }}
{{- $gatewayService = $val }}
{{- else }}
{{- $apiServices = append $apiServices $val }}
{{- end }}
{{- end }}
{{- end }}

<<<<<<< HEAD
# 1. Gateway Ingress (без rewrite)
{{- if $gatewayService }}
=======
{{- if $frontendService }}
>>>>>>> parent of 7e44108 (fix k8s)
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
<<<<<<< HEAD
  name: gateway-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ $gatewayService.name }}
                port:
                  number: {{ $gatewayService.port }}
{{- end }}

# 2. Фронтенд Ingress (без rewrite)
{{- if $frontendService }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
=======
>>>>>>> parent of 7e44108 (fix k8s)
  name: frontend-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $frontendService.name }}
                port:
                  number: {{ $frontendService.port }}
{{- end }}

<<<<<<< HEAD
# 3. API-сервисы с rewrite (все, кроме /api и /)
=======
{{- if $gatewayService }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ $gatewayService.name }}
                port:
                  number: {{ $gatewayService.port }}
{{- end }}

>>>>>>> parent of 7e44108 (fix k8s)
{{- if $apiServices }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-ingress
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          {{- range $apiServices }}
          {{- $path := trimSuffix "/" .publishUrl }}
          - path: {{ (printf "%s(/|$)(.*)" $path) | quote }}
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .name }}
                port:
                  number: {{ .port }}
          {{- end }}
{{- end }}